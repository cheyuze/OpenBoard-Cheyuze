# OpenBoard 车厘子定制版 1.7.20

本版进一步优化顶部工具栏布局及直线绘制操作。

## 本版改进

- 删除网页模式后，不再保留工具栏伸缩空白；OpenBoard、录制、历史记录和桌面按钮稳定保持在第一行。
- 录制入口改用与其他工具一致的可缩放图标，统一图标尺寸和文字基线。
- Shift 继续用于临时绘制直线，但不会再自动开启或关闭“吸附网格和角度”。
- 吸附功能只响应用户手动点击，松开 Shift 也不会关闭已经手动启用的吸附。

## 许可声明

本项目是基于 OpenBoard 的非官方定制版，按 GNU GPL v3 发布。对应源码将随 `v1.7.20` 标签提供。

## 安装包校验

SHA-256：`A2D8C78FE12E88A3BC49732342A79C980E2F893600612079B0B4EB5471AF0BB2`

---

# OpenBoard 车厘子定制版 1.7.19

本版调整顶部工具栏布局。

## 本版改进

- 将“OpenBoard”齿轮菜单移动到“录制”按钮左侧的空白区域。
- 调整伸缩间距位置，使 OpenBoard、录制、白板、历史记录和桌面按钮保持在同一行。
- 避免窗口宽度不足时 OpenBoard 菜单单独换到第二行。

## 许可声明

本项目是基于 OpenBoard 的非官方定制版，按 GNU GPL v3 发布。对应源码将随 `v1.7.19` 标签提供。

## 安装包校验

SHA-256：`75662A29BECC7A10CEEF4ABE6C3D5129CD05828CD365A4BA15E9F3E522CDC1CC`

---

# OpenBoard 车厘子定制版 1.7.18

本版进一步精简教学流程，并让白板在重启后连续恢复到上次工作位置。

## 本版改进

- 录制功能只保留本地 MP4 视频文件，移除“发布到内网”和“发布到 YouTube”的菜单、执行逻辑及构建组件。
- 每次打开程序时自动恢复上次使用的白板文档和上次停留的页面。
- 从旧版本首次升级且尚无“上次文档”记录时，自动打开最近更新的现有白板，不再额外创建空文档。
- 仅当本机没有任何可用白板时才创建新的空白文档。
- 移除主界面的内置网页浏览器按钮、工具栏和网页模式调用。
- 保留教程页及白板互动组件所需的内部网页渲染能力，不影响现有教学内容。

## 许可声明

本项目是基于 OpenBoard 的非官方定制版，按 GNU GPL v3 发布。对应源码将随 `v1.7.18` 标签提供。

## 安装包校验

SHA-256：`31C16FFC36C183EF4CA02CF3195C3AE3DEA3852327C1B77AAC093FAAFBFB8533`

---

# OpenBoard 车厘子定制版 1.7.17

本版集中解决录像覆盖安全和更新链路可信性问题，并整理此前尚未正式发布的中文与下载体验优化。

## 本版改进

- 录像覆盖已有文件时改用原子保存，新视频完整写入成功后才替换目标文件。
- 跨磁盘复制、磁盘空间不足或写入失败时，原有文件保持不变，录制源文件继续保留。
- 更新清单只从 GitHub 官方 HTTPS 地址读取，公共代理不再参与版本号、下载地址和 SHA-256 的下发。
- 安装包仍可通过多个国内可达代理下载，并使用可信清单中的 SHA-256 校验。
- 下载源无数据或持续低速约 10 秒时自动切换下一条链路。
- 补齐国内加速、百度网盘和低速切换相关简体中文文案。
- 启动说明中的联系方式统一修正为“通过知音楼联系我”。
- 修复桌面模式录制仍抓取白板背景的问题，改为录制真实电脑屏幕。
- 录制结束后弹出“保存录制视频”对话框，可选择保存位置和文件名，避免后续录制覆盖之前的文件。
- 取消另存为时保留已完成的视频，并使用唯一文件名保存。
- 录制控制面板改为独立置顶工具窗，进入桌面模式时会自动显示。
- 录制面板改为现代深色圆角样式，重新绘制录制、停止、暂停、播放和设置图标。
- 按钮鼠标悬浮提示会根据录制状态显示“录制”、“停止”、“暂停”或“播放”。
- 更新安装包优先使用国内可达的 GitHub 加速地址，不可用时自动切换备用地址和 GitHub 原始地址。
- 连续低速时自动放弃当前链路并切换下一个下载源。
- 下载完成后仍会校验 SHA-256，校验不通过的安装包不会运行。
- 更新时可选择国内加速源自动下载，或打开百度网盘分享页面手动下载。
- 录制另存为时取消操作会确认是否放弃本次录制，避免误操作丢失视频。
- 主界面的“文档”入口在中文界面中改为“历史记录”。
- 录制视频中的铅笔鼠标指针改为透明背景样式。

## 完整定制内容

- 默认窗口化启动与 16:9 教学画布。
- 录屏支持暂停、继续，并直接生成 H.264/AAC MP4。
- 录入鼠标指针，书写时使用铅笔光标。
- 新增 Shift 直线、常用形状工具和完整色盘。
- 补齐简体中文界面，启动页改为版本更新内容。
- 支持启动检查更新、自动下载、进度显示和安装包校验。

## 许可声明

本项目是基于 OpenBoard 的非官方定制版，按 GNU GPL v3 发布。对应源码即 `v1.7.17` 标签内容。

## 安装包校验

SHA-256：`CACA061DFB9F7C6264D5D5456B83784A53BA8749EB5B6B6FE533E70A23E50FB1`
